{
  "name": "Document",
  "type": "object",
  "properties": {
    "project_id": {
      "type": "string"
    },
    "client_id": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "file_url": {
      "type": "string"
    },
    "file_type": {
      "type": "string",
      "enum": [
        "pdf",
        "document",
        "spreadsheet",
        "other"
      ],
      "default": "pdf"
    },
    "review_link": {
      "type": "string",
      "description": "Optional external link (Google Doc, Sheet, etc.) for client review"
    },
    "report_month": {
      "type": "string",
      "description": "YYYY-MM"
    },
    "status": {
      "type": "string",
      "enum": [
        "draft",
        "awaiting_approval",
        "shared",
        "approved",
        "changes_requested"
      ],
      "default": "awaiting_approval"
    },
    "change_request_status": {
      "type": "string",
      "enum": [
        "pending",
        "in_review",
        "resolved"
      ],
      "default": "pending",
      "description": "Lifecycle status of the client's change request: pending (just requested), in_review (team working on it), resolved (team replied)"
    },
    "admin_comment": {
      "type": "string"
    },
    "history": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string"
          },
          "by": {
            "type": "string"
          },
          "note": {
            "type": "string"
          },
          "date": {
            "type": "string"
          }
        }
      }
    }
  },
  "required": [
    "project_id",
    "title",
    "file_url"
  ]
}